---
import { navLinks, siteConfig } from '@/utils/data';

const currentPath = Astro.url.pathname;
---

<header
  id="navbar"
  class="fixed top-0 left-0 right-0 z-50 transition-all duration-500 ease-out-expo navbar-base"
  data-component="Navbar"
>
  <div class="container-custom">
    <nav class="flex items-center justify-between h-20">

      <!-- Logo -->
      <a href="/" id="logo" class="logo relative z-10 flex items-center gap-3" data-component="Logo">
        <span class="logo-name">Ramon Riera</span>
        <span class="logo-divider hidden lg:block"></span>
        <span class="logo-role hidden lg:inline">Strategic Product Designer</span>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden lg:flex items-center gap-1">
        {navLinks.map((link) => (
          <a
            href={link.href}
            class:list={[
              'px-4 py-2 text-sm font-medium rounded-full transition-all duration-300',
              currentPath === link.href || (link.href !== '/' && currentPath.startsWith(link.href))
                ? 'text-primary-500 bg-primary-500/10'
                : 'text-neutral-500 hover:text-neutral-900',
            ]}
          >
            {link.label}
          </a>
        ))}
      </div>

      <!-- Actions area -->
      <div class="flex items-center gap-5">

        <a
          href={`mailto:${siteConfig.email}`}
          class="hidden sm:inline-flex items-center gap-2 px-5 py-2.5 bg-neutral-900 text-white text-sm font-medium rounded-full transition-all duration-300 hover:bg-neutral-800 hover:shadow-md hover:shadow-neutral-900/10 group/btn"
        >
          Hablemos
          <svg class="w-4 h-4 transition-transform duration-300 group-hover/btn:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>

        <!-- Mobile menu button -->
        <button
          id="mobile-menu-btn"
          class="lg:hidden relative z-10 p-2 rounded-lg hover:bg-neutral-100 transition-colors"
          aria-label="Abrir menú"
        >
          <div class="w-6 h-5 flex flex-col justify-between" id="hamburger">
            <span class="block w-full h-0.5 bg-neutral-900 transition-all duration-300 origin-left"></span>
            <span class="block w-full h-0.5 bg-neutral-900 transition-all duration-300"></span>
            <span class="block w-3/4 h-0.5 bg-neutral-900 transition-all duration-300 origin-left"></span>
          </div>
        </button>
      </div>
    </nav>
  </div>
</header>

<!-- Code Mode FAB -->
<button
  id="code-mode-fab"
  class="code-mode-fab"
  aria-label="Open terminal"
>
  <span class="code-mode-fab-cursor">&gt;_</span>
  <span class="code-mode-fab-text">CODE</span>
</button>

<!-- Mobile Navigation -->
<div
  id="mobile-menu"
  class="fixed inset-0 bg-white z-[60] flex flex-col items-center justify-center opacity-0 pointer-events-none transition-all duration-500 ease-out-expo overscroll-none mobile-menu-overlay"
>
  <!-- Close button -->
  <button
    id="mobile-menu-close"
    class="absolute top-6 right-6 p-2 rounded-lg hover:bg-neutral-100 transition-colors z-10"
    aria-label="Cerrar menú"
  >
    <svg class="w-7 h-7 text-neutral-900" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
    </svg>
  </button>
  <nav class="flex flex-col items-center gap-2">
    {navLinks.map((link, i) => (
      <a
        href={link.href}
        class:list={[
          'text-3xl font-bold tracking-tight transition-all duration-300 py-2',
          currentPath === link.href
            ? 'text-primary-500'
            : 'text-neutral-400 hover:text-neutral-900',
        ]}
        style={`transition-delay: ${i * 50}ms`}
      >
        {link.label}
      </a>
    ))}
  </nav>
  <a
    href={`mailto:${siteConfig.email}`}
    class="mt-6 btn-primary text-lg"
  >
    Hablemos
  </a>
</div>

<!-- Spacer for fixed navbar -->
<div class="h-20"></div>

<style>
  /* Navbar base — always glassmorphism-ready, safe-area aware */
  .navbar-base {
    padding-top: env(safe-area-inset-top, 0px);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    background: rgba(255, 255, 255, 0.85);
    transition: transform 0.35s cubic-bezier(0.22, 1, 0.36, 1),
                background 0.5s ease,
                box-shadow 0.5s ease;
    will-change: transform;
    contain: layout style;
  }

  .nav-scrolled {
    background: rgba(255, 255, 255, 0.88);
    box-shadow: 0 1px 8px rgba(0, 0, 0, 0.06);
  }

  /* Hide on scroll down */
  .navbar-hidden {
    transform: translateY(-100%);
  }

  .logo-name {
    font-family: 'Space Grotesk', system-ui, sans-serif;
    font-weight: 600;
    font-size: 1.5rem;
    line-height: 1;
    color: #1a1918;
    white-space: nowrap;
    letter-spacing: -0.03em;
  }

  .logo-divider {
    width: 1px;
    height: 1.1rem;
    background: rgba(0, 0, 0, 0.2);
    flex-shrink: 0;
  }

  .logo-role {
    font-family: 'Space Grotesk', system-ui, sans-serif;
    font-weight: 400;
    font-size: 0.8rem;
    letter-spacing: 0.05em;
    color: rgba(0, 0, 0, 0.45);
    white-space: nowrap;
  }

  /* ── Code Mode FAB — desktop only ── */
  .code-mode-fab {
    display: none;
    position: fixed;
    bottom: 1.5rem;
    right: 1.5rem;
    z-index: 50;
    align-items: center;
    gap: 0.5rem;
    padding: 0.65rem 1.1rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    font-weight: 700;
    color: #4ade80;
    background: #000;
    border: 1.5px solid #4ade80;
    border-radius: 10px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(74, 222, 128, 0.1);
    cursor: pointer;
    transition: transform 0.1s ease;
  }

  @media (min-width: 1024px) {
    .code-mode-fab {
      display: inline-flex;
    }
  }

  .code-mode-fab:hover {
    transform: scale(1.05);
  }

  .code-mode-fab:active {
    transform: scale(0.95);
  }

  .code-mode-fab-cursor {
    animation: fab-blink 0.8s step-end infinite;
  }

  .code-mode-fab-text {
    letter-spacing: 0.1em;
  }

  @keyframes fab-blink {
    0%, 49% { opacity: 1; }
    50%, 100% { opacity: 0; }
  }
</style>

<script>
  const navbar = document.getElementById('navbar');
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const hamburger = document.getElementById('hamburger');
  let isMenuOpen = false;

  // Scroll-hide state
  let lastKnownScroll = 0;
  let prevScroll = 0;
  const SCROLL_THRESHOLD = 8; // px before toggling visibility

  window.addEventListener('scroll', () => {
    if (isMenuOpen) return;

    lastKnownScroll = window.scrollY;

    if (navbar) {
      // Glassmorphism on scroll
      if (lastKnownScroll > 50) {
        navbar.classList.add('nav-scrolled');
      } else {
        navbar.classList.remove('nav-scrolled');
      }

      // Scroll-hide: hide on scroll down, show on scroll up
      const delta = lastKnownScroll - prevScroll;
      if (delta > SCROLL_THRESHOLD && lastKnownScroll > 100) {
        navbar.classList.add('navbar-hidden');
      } else if (delta < -SCROLL_THRESHOLD || lastKnownScroll < 50) {
        navbar.classList.remove('navbar-hidden');
      }
    }

    prevScroll = lastKnownScroll;
  }, { passive: true });

  const mobileMenuClose = document.getElementById('mobile-menu-close');

  let scrollPosition = 0;

  function lockScroll() {
    scrollPosition = window.scrollY;
    lastKnownScroll = scrollPosition;
    document.body.style.overflow = 'hidden';
    document.body.style.position = 'fixed';
    document.body.style.top = `-${scrollPosition}px`;
    document.body.style.width = '100%';
  }

  function unlockScroll() {
    document.body.style.overflow = '';
    document.body.style.position = '';
    document.body.style.top = '';
    document.body.style.width = '';
    document.documentElement.style.scrollBehavior = 'auto';
    window.scrollTo(0, scrollPosition);
    document.documentElement.style.scrollBehavior = '';

    if (navbar) {
      // Ensure navbar is visible after closing menu
      navbar.classList.remove('navbar-hidden');
      if (scrollPosition > 50) {
        navbar.classList.add('nav-scrolled');
      } else {
        navbar.classList.remove('nav-scrolled');
      }
    }
  }

  let isAnimating = false;

  function resetHamburger() {
    if (!hamburger) return;
    const spans = hamburger.querySelectorAll('span');
    spans[0].style.transform = '';
    spans[1].style.opacity = '1';
    spans[2].style.transform = '';
    spans[2].style.width = '75%';
  }

  function closeMenu() {
    if (!mobileMenu || !hamburger || isAnimating) return;
    isAnimating = true;
    isMenuOpen = false;
    mobileMenu.classList.add('pointer-events-none');
    mobileMenu.classList.add('opacity-0');
    mobileMenu.classList.remove('opacity-100');
    resetHamburger();

    const safetyTimer = setTimeout(() => {
      unlockScroll();
      isAnimating = false;
    }, 600);

    mobileMenu.addEventListener('transitionend', function handler(e) {
      if (e.propertyName !== 'opacity') return;
      mobileMenu.removeEventListener('transitionend', handler);
      clearTimeout(safetyTimer);
      unlockScroll();
      isAnimating = false;
    }, { once: false });
  }

  function openMenu() {
    if (!mobileMenu || !hamburger || isAnimating) return;
    isAnimating = true;
    isMenuOpen = true;
    lockScroll();
    mobileMenu.classList.remove('opacity-0', 'pointer-events-none');
    mobileMenu.classList.add('opacity-100');
    const spans = hamburger.querySelectorAll('span');
    spans[0].style.transform = 'rotate(45deg) translateY(2px)';
    spans[1].style.opacity = '0';
    spans[2].style.transform = 'rotate(-45deg) translateY(-2px)';
    spans[2].style.width = '100%';

    // Ensure navbar stays visible while menu is open
    if (navbar) navbar.classList.remove('navbar-hidden');

    const safetyTimer = setTimeout(() => {
      isAnimating = false;
    }, 600);

    mobileMenu.addEventListener('transitionend', function handler(e) {
      if (e.propertyName !== 'opacity') return;
      mobileMenu.removeEventListener('transitionend', handler);
      clearTimeout(safetyTimer);
      isAnimating = false;
    }, { once: false });
  }

  mobileMenuClose?.addEventListener('click', closeMenu);

  mobileMenuBtn?.addEventListener('click', () => {
    if (isMenuOpen) {
      closeMenu();
    } else {
      openMenu();
    }
  });

  mobileMenu?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', closeMenu);
  });

  // ── Code Mode FAB → open terminal ──
  const codeFab = document.getElementById('code-mode-fab');
  codeFab?.addEventListener('click', () => {
    if (typeof window.openTerminal !== 'function') return;
    codeFab.style.display = 'none';
    window.openTerminal();
  });

  // Show/hide FAB based on terminal state
  const termEl = document.getElementById('interactive-terminal');
  if (termEl) {
    new MutationObserver(() => {
      if (codeFab) codeFab.style.display = termEl.classList.contains('is-open') ? 'none' : '';
    }).observe(termEl, { attributes: true, attributeFilter: ['class'] });
  }
</script>
